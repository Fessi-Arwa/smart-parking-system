[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "sh -c 'gunicorn plate_api:app --bind 0.0.0.0:${PORT:-8080} --timeout 180'"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
